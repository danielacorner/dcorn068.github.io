index or layout index:
- find out what's causing that initial grid with border to appear

responsive:
- check mobile, tablet, wide desktop views
-desktop: set max-width of canvas to prevent link breaks
- size main page grid container
- size projects pages

deployed version:
-error occurs when path name incorrent (add /dc_gatsby/ until deployed to dcorn068.github.io)
- check styles when deployed
- button mouseover cursor pointer

finally:
- set as main github page
  pathPrefix: `/`,
https://www.gatsbyjs.org/docs/how-gatsby-works-with-github-pages/#github-organization-or-user-page

1.     "deploy": "gatsby build --prefix-paths && gh-pages -d public"
  pathPrefix: `/dc_gatsby`,
    "url": "https://dcorn068.github.io/dc_gatsby/"

2.     "deploy": "gatsby build && gh-pages -d public -b master"
    "url": "https://dcorn068.github.io/"
